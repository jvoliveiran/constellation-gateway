services:
  gateway:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "3000:3000"
    env_file: .env
    depends_on:
      - subgraph
    healthcheck:
      test: ["CMD", "wget", "--spider", "http://localhost:3000/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  subgraph:
    # TODO: Replace with your actual subgraph image (e.g. my-org/constellation-subgraph:latest).
    # The default node:20-alpine will exit immediately — it exists only to satisfy depends_on.
    image: ${SUBGRAPH_IMAGE:-node:20-alpine}
    command: ["echo", "Replace SUBGRAPH_IMAGE with your subgraph service"]
    ports:
      - "3001:3001"
